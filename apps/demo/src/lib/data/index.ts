/**
 * DAUI Data Utilities
 *
 * Batteries-included data fetching patterns for REST, polling, and WebSocket.
 *
 * @example
 * ```ts
 * import { createAsyncState, createPollingState, createWebSocket } from '$lib/data';
 * ```
 */

export * from './async.svelte';
export * from './polling.svelte';
export * from './websocket.svelte';
