/**
 * svelte-daui/media - Media preset export
 *
 * Use this to add video timeline and player components.
 *
 * @example
 * ```ts
 * import { PageRenderer, register } from 'svelte-daui';
 * import { media } from 'svelte-daui/media';
 *
 * register(media);
 * ```
 */

export { media } from './presets/media';
